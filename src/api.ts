import { Configuration, InternalApi, StripeApi } from './autogenerated';
import { API_URL } from './config';

const configuration = new Configuration({
  basePath: API_URL,
});

export const internalApi = new InternalApi(configuration);
export const stripeApi = new StripeApi(configuration);
